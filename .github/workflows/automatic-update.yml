name: Automatic AI Trends Update

on:
  schedule:
    - cron: "0 9 1,15 * *" # Každé 2 týdny (1. a 15. den měsíce) v 9:00 UTC
  workflow_dispatch: # Umožňuje manuální spuštění

permissions:
  contents: write
  pull-requests: write

jobs:
  update-trends:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          mv package.json package.json.bak || true
          npm install openai
          mv package.json.bak package.json || true

      - name: Generate automatic update
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          node scripts/generate-automatic-update.js

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: automatic AI trends update"
          branch: automated/trends-update-${{ steps.date.outputs.date }}
          title: "🤖 AI Trends Update - ${{ steps.date.outputs.date }}"
          body: |
            ## 🤖 Automatická aktualizace AI trendů

            Tento PR byl automaticky vygenerován GitHub Actions workflow.

            ### 📝 Změny
            - Aktualizace TRENDS.md s nejnovějšími trendy za poslední 2 týdny
            - Aktualizace MODEL-RECOMMENDATIONS.md s novými modely a cenami
            - Aktualizace README.md s aktuálním datem
            - Aktualizace CHANGELOG.md

            ### 🔍 Zkontroluj prosím
            - Správnost informací
            - Formátování markdown
            - Konzistenci s existujícím obsahem

            ---
            *Vygenerováno pomocí GPT-5.3-Codex*
          labels: |
            automated
            ai-trends
            documentation
