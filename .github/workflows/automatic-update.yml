name: Automatic AI Trends Update

on:
  schedule:
    # Spustí se každé 2 týdny (1. a 15. den měsíce) v 9:00 UTC
    - cron: "0 9 1,15 * *"
  workflow_dispatch: # Umožňuje manuální spuštění

jobs:
  update-trends:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Generate monthly update
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          node scripts/generate-monthly-update.js

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "🤖 Monthly AI trends update - $(date +'%B %Y')"
          git push

      - name: Create Pull Request
        if: steps.git-check.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "🤖 Monthly AI trends update - $(date +'%B %Y')"
          title: "🤖 AI Trends Update - $(date +'%B %Y')"
          body: |
            ## 📊 Měsíční aktualizace AI trendů

            Automaticky vygenerovaná aktualizace obsahující:
            - Nové modely a jejich benchmarky
            - Aktualizované frameworky a nástroje
            - Nové use cases a best practices
            - Aktualizované srovnávací tabulky

            **Datum:** $(date +'%d.%m.%Y')

            Prosím zkontroluj změny a merge pokud jsou v pořádku.
          branch: auto-update-$(date +'%Y-%m-%d')
          delete-branch: true
